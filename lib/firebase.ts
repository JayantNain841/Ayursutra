// Firebase is disabled for now - running in demo mode only
export const isFirebaseEnabled = false

// Placeholder exports for compatibility
export const auth = null
export const googleProvider = null
export const functions = null

console.log("ℹ️ Firebase is disabled - running in demo mode")
