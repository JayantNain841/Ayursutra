// Optional: Server-side Firebase Admin SDK configuration
// This file is for future server-side operations if needed

export const firebaseAdminConfig = {
  // Add admin SDK configuration here when needed
  // This would be used for server-side authentication verification
}
